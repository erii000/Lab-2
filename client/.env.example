# Leave unset in local dev to use Vite proxy (/api → gateway). Set for production builds.
# VITE_API_BASE_URL=http://localhost:5161

# Vite dev proxy target when API is not on 5161
# VITE_API_PROXY_TARGET=http://localhost:5161
