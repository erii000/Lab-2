# TravelPreferencesService (placeholder)

Not deployed. Travel preferences are implemented in **ItineraryService** (`TravelPreferences` table, `/api/v1/travel-preferences` routes).

This folder exists for the original team split backlog.
